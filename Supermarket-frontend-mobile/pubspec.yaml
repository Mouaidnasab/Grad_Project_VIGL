name: vigl
description: The third flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.6.0

dependencies:
  url_launcher: ^6.2.4
  camera: ^0.10.5+2 # or latest version
  mobile_scanner: ^3.5.2 # Use the latest version from pub.dev
  image_picker: ^1.1.0
  permission_handler: ^11.3.0

  flutter_secure_storage: ^8.0.0
  shared_preferences: ^2.0.0
  http: ^0.13.6

  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
